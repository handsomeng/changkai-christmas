# 敞开酒馆圣诞祝福墙 - 部署说明

## 🎉 恭喜！代码已准备就绪

你现在拥有一个完整的实时互动祝福系统：
- ✅ 大屏实时显示
- ✅ 手机扫码提交
- ✅ 云端数据同步
- ✅ 弹幕滚动播放

---

## 📋 第一步：创建 Supabase 数据表

### 1. 登录 Supabase
访问：https://supabase.com/dashboard

### 2. 选择你的项目
项目名：`uztdisifbsbeaephuvpu`

### 3. 执行 SQL（重要！）
点击左侧 **SQL Editor** → **New Query**，复制粘贴以下 SQL 并点击 **Run**：

```sql
-- 创建祝福表
create table blessings (
  id bigint primary key generated always as identity,
  name text,
  blessing text not null,
  created_at timestamp with time zone default now()
);

-- 开启实时功能（重要！）
alter publication supabase_realtime add table blessings;

-- 设置访问策略
alter table blessings enable row level security;

create policy "允许所有人查看祝福"
  on blessings for select
  to public
  using (true);

create policy "允许所有人发送祝福"
  on blessings for insert
  to public
  with check (true);
```

### 4. 验证
- 点击左侧 **Table Editor**
- 应该能看到 `blessings` 表
- 列：`id`, `name`, `blessing`, `created_at`

---

## 🚀 第二步：本地测试

### 1. 启动本地服务器
```bash
cd /Users/handsomeng/Downloads/vibe-coding/changkai-Christmas

# 使用 Python 启动（推荐）
python3 -m http.server 8000

# 或使用 Node.js
npx http-server -p 8000
```

### 2. 测试大屏
浏览器访问：http://localhost:8000/敞开圣诞树.html

**检查：**
- ✅ 左上角显示二维码
- ✅ 右上角有摄像头按钮
- ✅ 弹幕自动滚动
- ✅ 浏览器控制台显示：`✅ Subscribed to blessings in real-time`

### 3. 测试手机端
- 方式1：手机扫描二维码（需确保手机和电脑在同一 WiFi）
- 方式2：电脑浏览器打开：http://localhost:8000/手机端祝福.html

**测试提交祝福：**
1. 填写名字（可选）和祝福
2. 点击"送出祝福"
3. 大屏应该立即显示大字高亮 5 秒
4. 5 秒后变成弹幕滚动

---

## 🌐 第三步：部署到你的域名

### 方案 A：使用 Vercel（推荐，最简单）

#### 1. 准备 Git 仓库
```bash
cd /Users/handsomeng/Downloads/vibe-coding/changkai-Christmas
git init
git add .
git commit -m "敞开酒馆圣诞祝福墙"
```

#### 2. 推送到 GitHub
在 GitHub 创建新仓库，然后：
```bash
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

#### 3. 部署到 Vercel
1. 访问：https://vercel.com
2. 用 GitHub 登录
3. 点击 **New Project**
4. 选择你的仓库
5. 点击 **Deploy**
6. 等待部署完成（约 1 分钟）

#### 4. 绑定你的域名
1. 进入项目 → **Settings** → **Domains**
2. 添加你的域名（例如：christmas.你的域名.com）
3. 按照提示在你的域名 DNS 设置中添加 CNAME 记录
4. 等待生效（通常几分钟）

---

### 方案 B：传统服务器部署

#### 1. 上传文件
使用 FTP/SFTP 上传以下文件到服务器：
```
你的域名/christmas/
├── 敞开圣诞树.html
├── 手机端祝福.html
├── 敞开logo.jpg
└── config.js
```

#### 2. 配置 Nginx（如果需要）
```nginx
location /christmas/ {
    index 敞开圣诞树.html;
    try_files $uri $uri/ =404;
}
```

#### 3. 访问
- 大屏：https://你的域名.com/christmas/敞开圣诞树.html
- 手机：https://你的域名.com/christmas/手机端祝福.html

---

## 📱 第四步：现场使用

### 准备工作
1. 打开大屏页面（建议用大显示器或投影）
2. 按 **F11** 全屏显示
3. 确保网络稳定

### 用户流程
1. 用户扫描左上角二维码
2. 填写祝福并提交
3. 大屏立即高亮显示 5 秒
4. 转为弹幕循环播放

### 可选功能
- 点击右上角 📷 启动手势控制
  - 握拳：树聚拢
  - 张开：树散开
  - 移动手：旋转视角

---

## 🔧 故障排查

### 问题1：二维码扫不了
**原因**：本地测试时 localhost 手机无法访问

**解决**：
1. 查看电脑 IP：`ifconfig` (Mac) 或 `ipconfig` (Windows)
2. 修改 `敞开圣诞树.html` 中的 `generateQRCode()` 函数：
   ```javascript
   const mobileUrl = 'http://你的IP:8000/手机端祝福.html';
   ```

### 问题2：提交祝福没反应
**原因**：Supabase 表未创建或权限未设置

**检查**：
1. 浏览器控制台是否有错误
2. Supabase → Table Editor 是否有 `blessings` 表
3. Supabase → Authentication → Policies 是否已创建策略

### 问题3：不实时显示
**原因**：实时订阅未开启

**解决**：
1. 执行 SQL：`alter publication supabase_realtime add table blessings;`
2. Supabase → Database → Replication 中检查 `blessings` 是否勾选

---

## 📊 数据管理

### 查看所有祝福
Supabase → Table Editor → `blessings` 表

### 清空祝福数据
```sql
truncate table blessings;
```

### 导出数据
Table Editor → 右上角 **...** → **Export to CSV**

---

## 💰 成本说明

- **Supabase**: 免费（5万次读写/月）
- **Vercel**: 免费（100GB 流量/月）
- **预估**：100 人参与 ≈ 200 次数据库操作，完全免费

---

## 🎨 自定义建议

### 修改颜色
在 CSS 中搜索 `#D4AF37`（金色）替换为你想要的颜色

### 修改祝福字数限制
- 手机端：修改 `maxlength="30"`
- 大屏端：无需修改（自动适配）

### 添加审核功能
在 `blessings` 表添加 `is_approved` 字段，只显示审核通过的祝福

---

## 📞 需要帮助？

遇到问题随时问我！

祝你圣诞活动成功！🎄✨

